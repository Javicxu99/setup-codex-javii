# Bootstrap this repo

Act as a senior engineer and improve `setup-codex-javii` as a personal Codex bootstrapper.

Goals:

- Keep the `setup-codex-javii` skill simple, reusable, and dependency-free.
- Verify that preview mode is read-only and changed files receive backups before atomic replacement.
- Verify that identical repeats create no extra backups and selective components stay isolated.
- Keep templates short, useful, and action-oriented.
- Validate preview and apply in a temporary project using `--profile default`.
- Confirm the manual Codex Desktop health prompt requires no API key or external automation.
- Keep `CHANGELOG.md`, `VERSION`, and GitHub templates aligned with release intent.

Do not commit unless explicitly asked.
